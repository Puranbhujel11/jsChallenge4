let change = function(coins, amounts) {
    let table = new Array(amounts + 1). fill(infinity);
    table[0] = 0; 

    for(let i = 0; i < table.length; i++) {
        if(coins <= i) {
            let 
        }
    }
}